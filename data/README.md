# Data

This directory is for local PhoneWatch datasets and quick-test media.

The generated dataset folders are ignored by git:

- `raw/`
- `processed/`
- `augmented/`

Run the setup pipeline when you need to rebuild local data:

```bash
python run.py setup
```
